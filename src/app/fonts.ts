import { Spectral } from "next/font/google";

export const font_secundary = Spectral({
  weight: "700",
  style: "normal",
  subsets: ["latin"],
  variable: "--font-spectral",
  display: "swap",
});
